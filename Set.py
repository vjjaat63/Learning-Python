# collection of unordered items which are unique and are immutable so we can't store list and dictionary in it

collection = {1,2,3,4,5,5,5,"hello",8,"hello",6,3}   # only values not key value pairs
print(collection)
print(len(collection))

#empty set

data = {}  # this is dictionary
data2 = set() #empty set

# method - add,remove , clear but can't change